import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GaleryScreen = () => {
  return (
    <View>
      <Text>Restaurant Galery Screen</Text>
    </View>
  );
};

export default GaleryScreen;

const styles = StyleSheet.create({});
